<Window x:Class="ChatRWKV_PC.Views.UpgradeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatRWKV_PC"
        mc:Ignorable="d"
        Title="{DynamicResource Lang_UpdateLibTitle}" Height="120" Width="200" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="AllVersionComboBox" SelectedIndex="0" SelectionChanged="AllVersionComboBox_SelectionChanged" Margin="5"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Content="{DynamicResource Lang_OK}" Width="85" Grid.Column="0" x:Name="ConfirmBtn" Click="ConfirmBtn_Click" />
            <Button Content="{DynamicResource Lang_Cancel}" Width="85"  Grid.Column="1" x:Name="CancelBtn" Click="CancelBtn_Click"/>
        </Grid>
    </Grid>
</Window>
